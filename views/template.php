<!DOCTYPE html>
<html lang="nl">
    <head>
        <title>my new website</title>
    </head>
    <body>
        {{ print test }}
        {{ if 3 less 4 }}
            <h1>{{ title }}</h1>
        {{ end }}

        <p>{{ page_content }}</p>

        <h3>{{ description }}</h3>
    </body>
</html>
