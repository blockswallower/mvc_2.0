<!DOCTYPE html>
<html lang="nl">
    <head>
        <title>my new website</title>
    </head>
    <body>
        {{ if 4 !equals 3 }}
            <h1>{{ title }}</h1>
        {{ end }}

        <p>{{ page_content }}</p>

        <h3>{{ description }}</h3>
    </body>
</html>
